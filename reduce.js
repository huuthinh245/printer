// var arr = ['a', 'b', 'c', 'b', 'e', 'a'];

// var arr1 = arr.reduce((allNames, name) => {
//   if (name in allNames) {
//     console.log(allNames[name]);
//     allNames[name]++;
//   } else {
//     allNames[name] = 1;
//   }

//   return allNames;
// }, []);

// console.log(arr1);

// const a = [{ price: 1000 }];
// a[0].price = a[0].price.toLocaleString();
// console.log(a);

const a = [1, 2, 3, 4];
console.log(...a);
console.log(Math.max(...a));
